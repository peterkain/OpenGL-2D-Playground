#include "Coin.h"


Coin::Coin(const Vector3& pos, const GLfloat& angle, const Vector3& rotation, const Vector2& size,
           const Vector2& vel, const Vector4& color)
    :   Entity(pos, angle, rotation, size, vel, color)
{

}


